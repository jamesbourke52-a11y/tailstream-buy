{
  "name": "tailstream-buy",
  "version": "0.1.0",
  "private": true,
  "description": "Tailstream one-tap non-custodial buy — Solana Action / Blink endpoint (Jupiter swap builder).",
  "dependencies": {
    "@solana/actions": "^1.6.0"
  },
  "engines": { "node": ">=18" }
}
